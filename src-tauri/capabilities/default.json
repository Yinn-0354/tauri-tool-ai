{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限:核心 + dialog",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
